# Python 3.12 / Ubuntu 24.04
paho-mqtt>=2.1          # 2.x callback API (VERSION2)
openai>=1.40
httpx>=0.27             # streaming TTS (SSE) — openai 也依赖它，这里显式声明
numpy>=1.26
websocket-client>=1.8
python-dotenv>=1.0
